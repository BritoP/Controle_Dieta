<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_dieta"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_dieta_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.ControleDieta"
        tools:targetApi="31">

        <activity
            android:name=".AlimentosActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AlimentoActivity"
            android:exported="false"
            android:parentActivityName=".AlimentosActivity" />

        <activity
            android:name=".AutoriaActivity"
            android:exported="false"
            android:parentActivityName=".AlimentosActivity" />

    </application>

</manifest>